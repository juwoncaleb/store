import React from 'react'

// AUTHENTICATION
import { useSession, signIn, signOut } from "next-auth/react"
export default function auth() {
    const { data: session } = useSession()
    return (
        <div>
            <button className="text-7xl headertext">GOOGLE</button>
{if}
        </div>
    )
}
